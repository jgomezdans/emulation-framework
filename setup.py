#!/usr/bin/env python

from setuptools import setup

setup(name='multiply-emulation-framework',
      version='0.1',
      description='MULTIPLY Emulation Framework',
      author='MULTIPLY Team',
      packages=['multiply_emulation_engine'],
     )